from setuptools import setup, find_packages

setup(
    name='document-extraction',  # This is the name of your PyPI-package.
    version='0.1',  # Update the version number for new releases
    packages=find_packages()  # find all packages to be exported
)
